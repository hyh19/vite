# Vite Mochi 学习卡片

## 什么是 Vite？

Vite（法语中"快"的意思，发音为 /vit/）是一个现代化的前端构建工具，由 Vue.js 的创建者尤雨溪开发。它主要由两部分组成：

1. 一个基于原生 ES 模块的开发服务器，提供丰富的内置功能和极快的热模块替换 (HMR)
2. 一个使用 Rollup 打包代码的构建命令

费曼式解释：想象你正在建造一座乐高城堡。传统工具（如 Webpack）会在你开始玩之前，先把所有乐高积木粘在一起，这需要等待很长时间。而 Vite 则像一个聪明的助手，只会在你需要某个积木时立即递给你，不需要预先粘合所有积木。这样你可以立即开始建造，并且当你更换一个积木时，它只替换那一个积木而不是整个城堡，让你的建造过程变得超级快速。

## Vite 与传统打包工具的主要区别是什么？

Vite 与传统打包工具（如 Webpack）的主要区别在于：

1. 开发环境下，Vite 利用浏览器原生 ES 模块支持，实现了无打包开发服务器，只在浏览器请求模块时进行按需编译
2. Vite 将依赖分为依赖和源码两类分别处理：
   - 依赖通常是不会变动的纯 JavaScript，Vite 会使用 esbuild 进行一次性预构建
   - 源码通常包含非 JavaScript 文件，需要转换，且会频繁变动，Vite 采用按需编译
3. 生产环境下，Vite 使用 Rollup 进行打包，提供了一套构建优化的默认配置

费曼式解释：传统工具就像准备一顿自助餐 - 即使客人还没来，你也得先把所有菜都做好摆在桌上。而 Vite 则像是一家快餐店 - 只有当客人点了某道菜，厨师才会立即烹饪那一道菜。随着菜单（代码）越来越大，自助餐的准备时间会越来越长，但快餐店始终能快速响应顾客的点单。这就是为什么随着项目变大，Vite 的启动速度优势会更加明显。

## Vite 的依赖预构建是什么？

Vite 的依赖预构建是指在开发服务器启动时，Vite 使用 esbuild 预先处理项目依赖的过程。这一步骤主要解决两个问题：

1. **CommonJS 和 UMD 兼容性**：将非 ESM 格式的依赖项转换为 ESM 格式，因为 Vite 的开发服务器只支持 ES 模块
2. **性能优化**：将有许多内部模块的 ESM 依赖项转换为单个模块，减少浏览器请求数量

预构建的依赖会缓存在 `node_modules/.vite` 目录下。当以下情况发生时，缓存会失效：

- package.json 的 `dependencies` 字段变化
- vite.config.js 中与依赖相关的配置变化
- lockfile 如 `package-lock.json` 变化

费曼式解释：想象你去超市购物，传统方式是把你需要的每个商品单独结账（每个模块一个请求）。而 Vite 的依赖预构建就像是把相关商品打包成套装，一次性结账（将多个小模块合并为一个大模块）。同时，它还会把"外国货"（CommonJS/UMD）转换成"本地货"（ESM），让浏览器能够直接使用。这样不仅节省了购物时间，还让整个过程更加流畅。

## 什么是 Vite 的热模块替换（HMR）？

热模块替换（Hot Module Replacement，HMR）是 Vite 提供的一项核心功能，它允许在应用运行时替换、添加或删除模块，而无需完全刷新页面。Vite 的 HMR 具有以下特点：

1. Vite 通过原生 ESM 提供 HMR API，框架可以利用此 API 提供精确的更新
2. 当模块发生变化时，Vite 只需精确地使失效从编辑模块到其最近 HMR 边界之间的链（大多数时候只是模块本身）
3. 无论应用大小如何，HMR 更新速度都保持一致

Vite 为多个框架提供了开箱即用的 HMR 集成：

- Vue 单文件组件
- React 通过 Fast Refresh
- Preact 通过官方集成
- Svelte 等其他框架

要在自定义设置中使用 HMR，可以通过 `import.meta.hot` API 手动处理模块热替换。

费曼式解释：想象一下，你正在编辑一篇文档。传统方式是，每当你改动一个段落，整个文档都要重新打印一遍。而 Vite 的热模块替换就像是使用了神奇的纸张 - 当你修改某个段落时，只有那个段落会被替换掉，文档的其他部分和你的阅读位置都保持不变。这样你可以立即看到修改的效果，而不会打断你的思路或丢失当前状态。

## Vite 如何处理 TypeScript 代码？

Vite 对 TypeScript 提供了开箱即用的支持，主要特点如下：

1. Vite 使用 esbuild 将 TypeScript 转译为 JavaScript，速度比传统的 `tsc` 快约 20-30 倍
2. Vite 只执行转译而不进行类型检查，假定类型检查由 IDE 和构建过程处理
3. 在生产构建时，可以运行 `tsc --noEmit` 命令来检查类型
4. 开发过程中，建议在单独的进程中运行 `tsc --noEmit --watch` 或使用 vite-plugin-checker 插件

使用 TypeScript 时，需要注意以下配置选项：

- 将 `compilerOptions.isolatedModules` 设置为 true
- `useDefineForClassFields` 默认值取决于 `target` 配置
- Vite 会忽略 `tsconfig.json` 中的 `target` 配置，开发时默认使用 esnext

对于客户端类型定义，可以添加 `vite/client` 类型引用。

费曼式解释：TypeScript 就像是给 JavaScript 添加了标签和说明书的系统。Vite 处理 TypeScript 时，它不会细读所有的说明书（不做类型检查），而是直接把带标签的部分转换回普通 JavaScript（转译）。这就像是把一本带有详细注释的菜谱简化成基本步骤 - 菜肴（代码）的功能不变，但去掉了额外的说明信息（类型）。这种方式让开发速度快得多，而把仔细检查说明书的工作（类型检查）交给了专门的工具（IDE 和单独的类型检查流程）。

## Vite 的环境变量和模式是如何工作的？

Vite 使用 dotenv 从环境目录中的特定文件加载额外的环境变量。环境变量的处理规则如下：

1. `.env` 文件是所有情况下都会加载的环境变量
2. `.env.local` 包含本地环境变量，会被 git 忽略
3. `.env.[mode]` 文件只在特定模式下加载（如 `.env.production`）
4. `.env.[mode].local` 文件同上，但会被 git 忽略

加载优先级从低到高为：
`.env` → `.env.local` → `.env.[mode]` → `.env.[mode].local`

只有以 `VITE_` 为前缀的变量才会暴露给客户端源码，可以通过 `import.meta.env` 对象访问。

Vite 还提供了几个内置的环境变量：

- `import.meta.env.MODE`: 应用运行的模式
- `import.meta.env.BASE_URL`: 部署的基本 URL
- `import.meta.env.PROD`: 是否为生产环境
- `import.meta.env.DEV`: 是否为开发环境
- `import.meta.env.SSR`: 是否为服务器端渲染

费曼式解释：环境变量就像是不同场景下的配置开关。想象你有一个玩具，在家里玩和在户外玩需要不同的设置。Vite 的环境变量系统就像是一系列贴在玩具上的便签，告诉它在不同地方该如何工作。`.env` 文件是基础设置（适用于所有地方），而 `.env.production` 则是特定场景的额外设置（只在生产环境使用）。为了安全起见，只有贴有特殊标记 "VITE_" 的便签才会被带到"户外"（客户端代码），其他的都保留在"家里"（服务器端）。

## Vite 的构建过程有哪些特点？

Vite 的生产构建过程依赖 Rollup，并提供了许多优化功能：

1. **多页面应用（MPA）支持**：可以指定多个 HTML 入口点
2. **库模式**：用 `build.lib` 配置构建可重用的库
3. **CSS 代码分割**：默认情况下，在异步 chunk 中导入的 CSS 会被提取为单独的文件
4. **预加载指令生成**：自动生成 `<link rel="modulepreload">` 指令
5. **异步 Chunk 加载优化**：使用动态导入时自动分割代码

常用的构建配置选项包括：

- `build.target`：指定浏览器兼容性目标
- `build.outDir`：指定输出目录，默认为 `dist`
- `build.assetsDir`：指定静态资源目录，默认为 `assets`
- `build.assetsInlineLimit`：小于此阈值的资源将内联为 base64
- `build.cssCodeSplit`：是否启用 CSS 代码分割
- `build.sourcemap`：是否生成源码映射

可以通过 `build.rollupOptions` 进一步自定义底层的 Rollup 打包行为。

费曼式解释：Vite 的构建过程就像是一个专业的打包服务。想象你要搬家，开发阶段就像是物品散落在家里各处，你需要什么就拿什么（按需加载）。而构建阶段则是正式搬家，Vite 会帮你把所有物品分类整理（代码分割），打包装箱（模块打包），压缩空间（压缩代码），并贴上标签（生成源码映射）。对于特别常用的小物品，它甚至会直接放在箱子内部而不是单独包装（资源内联），这样一到新家就能立即使用（加载更快）。

## Vite 的静态资源处理有哪些特点？

Vite 提供了多种方式来处理和引用静态资源：

1. **导入为 URL**：导入静态资源会返回解析后的URL，例如导入图片文件。

2. **导入为字符串**：添加 `?raw` 后缀可以将资源导入为字符串，适用于导入着色器或其他文本资源。

3. **导入为 Worker**：添加 `?worker` 或 `?sharedworker` 后缀可以将脚本导入为 Web Worker。

4. **导入脚本作为 Worker**：使用 `?url` 后缀获取资源 URL 而不是导入它的内容。

5. **公共目录**：放在 `public` 目录下的资源不会被处理，但可以通过绝对路径引用，比如网站图标。

6. **原生 URL 构造函数**：使用原生 JS URL 构造函数引用资源的方式也受到支持。

静态资源处理的配置选项：

- `assetsInclude`：指定额外的静态资源类型
- `build.assetsInlineLimit`：小于此值的资源会被内联为 base64 URL

费曼式解释：Vite 的静态资源处理就像是一个智能图书馆系统。当你需要一本书（静态资源）时，有多种方式可以获取它。你可以借出实体书（URL 引用），也可以获取电子版全文（字符串导入），或者只需要书的索引卡片（URL 导入）。对于特别常用的参考资料，可以放在专门的"公共阅览区"（public 目录），让所有人直接查阅。图书馆系统足够智能，能根据书的大小和使用方式，决定是给你完整的书（大文件），还是复印几页给你（小文件内联为 base64）。

## Vite 的插件系统是如何工作的？

Vite 的插件系统主要基于 Rollup 的插件接口，同时添加了 Vite 特定的扩展：

1. Vite 插件可以使用标准的 Rollup 插件 API
2. Vite 特有钩子可以处理特定的开发服务器功能，如 HMR

插件应用规则：

- 在开发中，Vite 开发服务器会调用插件的 Rollup 构建钩子
- 某些钩子如 `transform` 在两种模式下都会调用
- 使用 `enforce` 修饰符可以控制插件的执行顺序
  - `pre`：在 Vite 核心插件之前
  - 默认：在 Vite 核心插件之后
  - `post`：在 Vite 构建插件之后

常见的 Vite 特有钩子：

- `configureServer`：配置开发服务器
- `transformIndexHtml`：转换 HTML 内容
- `handleHotUpdate`：自定义 HMR 更新处理

条件应用插件：可以使用 `apply` 属性指定插件仅在 'build' 或 'serve' 模式下应用。

费曼式解释：Vite 的插件系统就像是一条生产流水线，原材料（源代码）沿着流水线移动，每个工作站（插件）可以检查、修改或转换经过的材料。有些工作站（Rollup 插件）负责通用加工任务，而另一些（Vite 特定插件）则处理特殊的快速原型开发功能。工作站可以安排在流水线的不同位置 - 前端（pre）、中间（默认）或后端（post）。有些工作站只在制作样品（开发环境）时工作，而另一些则只在生产最终产品（构建过程）时启用。整个系统的设计使得可以灵活地添加、移除或重新排列工作站，以适应不同的生产需求。

## Vite 的 SSR（服务端渲染）支持有哪些特点？

Vite 提供了内置的服务端渲染（SSR）支持，主要特点包括：

1. **灵活性**：Vite 提供底层 API 用于 SSR，而不是固定框架
2. **依赖处理**：SSR 过程中，Vite 自动外部化CommonJS 依赖（用 `require` 加载）和ESM 依赖（用动态导入预加载）
3. **源码转换**：与客户端开发类似，依赖预构建、CSS 处理等功能在 SSR 中也可用

使用 Vite 进行 SSR 开发的基本流程：

1. 创建一个服务器入口文件
2. 在开发中编写逻辑来加载和渲染虚拟模块 `*.js?url` 或 `import.meta.url`
3. 添加预加载指令优化生产环境加载性能
4. 配置部署前的预渲染步骤

SSR 配置选项：

- `server.middlewareMode`：以中间件模式运行 Vite 服务器
- `ssr.external`：指定始终外部化的依赖
- `ssr.noExternal`：指定不外部化的依赖
- `ssr.target`：SSR 服务器构建目标 (`node`、`webworker` 等)

费曼式解释：服务端渲染（SSR）就像是一家可以同时在店内用餐和外卖的餐厅。Vite 的 SSR 支持提供了厨房设备（API），但不规定你必须按某种方式烹饪（灵活性）。当顾客（用户）来访时，餐厅（服务器）先准备好餐点（渲染HTML），然后将其送到顾客桌前（发送到浏览器）。顾客坐下后，可以继续点更多的菜（客户端激活与交互）。这种方式让顾客能更快地看到并使用基本餐点（首屏内容），同时厨房可以继续准备更复杂的菜品（延迟加载的内容）。

## Vite 如何支持 PWA 开发？

Vite 通过官方维护的 vite-plugin-pwa 插件支持渐进式 Web 应用程序（PWA）开发：

1. 使用该插件可以自动生成 Service Worker 和 Web Manifest
2. 支持离线缓存、推送通知和安装体验等 PWA 功能
3. 可与任何前端框架集成（Vue、React、Svelte 等）

基本配置包括注册类型、应用名称、图标等信息，可以通过 vite.config.js 文件中的 VitePWA 插件进行设置。

PWA 功能支持：

- 自动注册 Service Worker
- 离线缓存应用资源
- 安装提示添加到主屏幕
- 自动更新检测新版本

费曼式解释：想象你最喜欢的网站可以像手机应用一样工作 - 这就是 PWA 的核心。Vite 的 PWA 支持就像是给网站穿上了一件特殊外套，让它即使在断网情况下也能工作（离线缓存），还能发送通知（推送通知），甚至可以"安装"到你的设备上（添加到主屏幕）。这件外套（Service Worker）在后台悄悄工作，帮网站存储重要数据，管理在线/离线状态的切换，并在有新内容时通知你。使用 Vite 的 PWA 插件，就像是让一位专业裁缝（自动化工具）为你的网站量身定制这件神奇的外套，而不必自己学习复杂的裁剪技术。

## Vite 配置文件有哪些特点和使用方式？

Vite 配置文件（通常为 `vite.config.js`）具有以下特点：

1. 支持 ES 模块语法，即使项目不使用原生 Node ESM
2. 可以使用 TypeScript 编写配置文件（`vite.config.ts`）
3. 提供 `defineConfig` 辅助函数获得类型提示
4. 支持条件配置和异步配置

基本配置结构包括各种选项，如插件、解析设置、CSS处理、服务器配置和构建选项等。

条件配置能根据当前命令（serve或build）和模式提供不同的配置。

常用配置类别：

- `plugins`：使用的 Vite 插件
- `resolve`：模块解析选项
- `css`：CSS 处理选项
- `server`：开发服务器选项
- `build`：构建选项
- `optimizeDeps`：依赖优化选项

费曼式解释：Vite 的配置文件就像是一张详细的菜单，告诉厨师（Vite）如何准备你的餐点（项目）。你可以用不同的语言写这个菜单（JS 或 TS），甚至可以根据是午餐还是晚餐（开发或生产环境）提供不同的选项。`defineConfig` 辅助函数就像是一位了解所有可能菜品的服务员，当你写菜单时给你提示。而且，如果某些菜品需要特殊准备（异步操作），菜单也能处理这种情况。整个系统设计得灵活而强大，让你能精确控制每道菜的烹饪方式，同时又不必了解厨房的所有细节。

## Vite 的开发服务器有哪些主要配置选项？

Vite 开发服务器提供了多种可配置选项来满足不同的开发需求：

1. **基本选项**：
   - `server.host`：指定服务器监听的 IP 地址，设置为 `0.0.0.0` 或 `true` 可监听所有地址
   - `server.port`：指定服务器端口，默认为 `5173`
   - `server.strictPort`：设置为 `true` 时，如果端口已被占用则直接退出
   - `server.https`：启用 TLS + HTTP/2

2. **高级选项**：
   - `server.cors`：配置 CORS，默认允许本地请求
   - `server.proxy`：设置自定义代理规则
   - `server.hmr`：配置热模块替换连接
   - `server.watch`：传递给 `chokidar` 的文件系统监视器选项
   - `server.middlewareMode`：以中间件模式运行 Vite

3. **开发便利性选项**：
   - `server.open`：服务器启动时自动在浏览器中打开应用
   - `server.headers`：指定服务器响应的自定义标头

代理配置可以将特定路径的请求转发到其他服务器，方便前后端分离开发。

费曼式解释：Vite 的开发服务器就像是一个可以完全定制的餐厅。你可以决定这个餐厅坐落在哪条街（host）、门牌号是多少（port）、是否提供户外座位（cors）、是否有专人接待客人（open）等。如果客人想要一些餐厅本身不提供的特殊食物，你甚至可以安排从附近餐厅取餐（proxy）。餐厅还配备了先进的送餐系统（hmr），当厨房做好新菜品时，服务员只会更换客人桌上的那一道菜，而不会打扰整个用餐体验。所有这些设置都可以根据你的喜好和需求进行调整，创造出最适合你的开发环境。

## Vite 构建优化配置有哪些主要选项？

Vite 提供了多种构建优化选项，可以通过 `build` 配置对象进行设置：

1. **输出相关配置**：
   - `build.target`：浏览器兼容性目标，默认为 `'modules'`
   - `build.outDir`：输出目录，默认为 `'dist'`
   - `build.assetsDir`：静态资源目录，默认为 `'assets'`
   - `build.assetsInlineLimit`：小于此值的资源会被内联为 base64 URL

2. **代码优化配置**：
   - `build.minify`：使用的压缩器，可选 `'terser'` 或 `'esbuild'`
   - `build.terserOptions`：传递给 Terser 的选项
   - `build.cssCodeSplit`：启用/禁用 CSS 代码分割
   - `build.cssMinify`：CSS 压缩器选项
   - `build.sourcemap`：是否生成源码映射

3. **高级和性能配置**：
   - `build.chunkSizeWarningLimit`：chunk 大小警告的限制（KB）
   - `build.reportCompressedSize`：是否报告压缩后的文件大小
   - `build.rollupOptions`：自定义底层的 Rollup 打包配置
   - `build.watch`：开启滚动构建（与 Rollup 的 watch 模式相同）

特殊模式配置：

- `build.lib`：构建为库
- `build.manifest`：生成包含非哈希资源文件名到其哈希版本的映射的清单文件

费曼式解释：构建优化就像是把一座大房子（你的应用）打包准备搬迁。Vite 的构建优化配置让你决定如何打包每一件物品。你可以选择把房子搬到什么样的新社区（target），在哪里放置家具（outDir），如何整理小物件（assetsDir），是否把一些非常小的物品直接放在大箱子里而不是单独包装（assetsInlineLimit）。你还可以决定是否压缩家具以节省空间（minify），如何分类整理衣物（cssCodeSplit），以及是否保留一份详细的物品清单以便日后查找（sourcemap）。通过这些设置，Vite 帮助你创建一个既紧凑又组织良好的应用，在新环境中能够高效运行。

## Vite 依赖优化的配置有哪些主要选项？

Vite 提供了依赖优化相关的配置选项，可通过 `optimizeDeps` 对象进行设置：

1. **控制预构建范围**：
   - `optimizeDeps.include`：始终进行预构建的依赖项
   - `optimizeDeps.exclude`：排除预构建的依赖项
   - `optimizeDeps.entries`：指定自定义条目，用于依赖发现

2. **细粒度控制**：
   - `optimizeDeps.force`：强制依赖预构建，忽略之前的缓存
   - `optimizeDeps.esbuildOptions`：传递给 esbuild 的自定义选项
   - `optimizeDeps.disabled`：完全禁用依赖优化

3. **预构建钩子**：
   - `optimizeDeps.needsInterop`：指定强制使用 dynamic import 交互操作的包

当以下情况发生时应考虑使用 `include`：

- 动态导入的依赖项（如 `import('package')`）
- 包含 `.vue`、`.jsx` 等非普通 JS 文件的依赖项
- 裸模块导入的 CommonJS 依赖项

费曼式解释：依赖优化就像是飞机起飞前的准备工作。正常情况下，飞机（Vite）会自动检查并准备所有必要的设备和燃料（依赖项）。但有时，你可能需要明确告诉机组人员某些特殊设备必须装载（include），或者某些常规设备这次不需要（exclude）。有时候，即使前一次飞行刚刚结束，你也可能需要重新检查所有设备（force）。`optimizeDeps` 配置就是让你能够精确控制这个准备过程，确保飞机能够顺利起飞并高效飞行，而不是在半空中才发现缺少必要的组件。

## 如何使用 Vite 进行前后端集成开发？

Vite 可以与多种后端框架集成，主要有以下几种方式：

1. **使用 Vite 作为开发服务器**：
   - 后端服务器将特定路径（如 `/assets/*`）代理到 Vite 服务器
   - HTML 页面由后端服务器提供，但引用 Vite 处理的资源

2. **使用 Vite 作为中间件**：
   - 在开发过程中，使用 `server.middlewareMode` 将 Vite 作为中间件集成到后端服务器
   - 生产环境使用 `vite build` 构建静态文件

3. **使用后端框架的官方 Vite 集成插件**：
   - Laravel 的 @vitejs/plugin-vue
   - Ruby on Rails 的 vite_ruby
   - Django 的 django-vite

前后端集成的关键配置：

- `build.manifest`：生成资产清单，帮助后端框架查找正确的资源路径
- `server.proxy`：在开发过程中将 API 请求代理到后端服务器
- `base`：设置部署的基本公共路径，确保资源引用正确

费曼式解释：前后端集成就像是两个不同的团队（前端和后端）在同一个项目上协作。Vite 提供了几种让这两个团队顺畅合作的方式。第一种方式是"分区工作"，每个团队在自己的区域工作，通过代理互相访问对方的成果。第二种是"嵌入合作"，将前端团队（Vite）作为后端团队的一个特殊成员（中间件）直接融入工作流程。第三种是使用专门的"协作指南"（官方集成插件），这些指南已经定义好了两个团队如何最佳地合作。无论使用哪种方式，都需要一些特殊的配置来确保沟通顺畅，就像是确保两个团队使用相同的项目图纸和坐标系统。

## Vite 如何处理浏览器兼容性问题？

Vite 处理浏览器兼容性的方式分为开发和生产两个环境：

1. **开发环境**：
   - Vite 将 `esnext` 作为转换目标，假定使用现代浏览器
   - 不进行语法降级，以最接近源代码的方式提供模块

2. **生产环境**：
   - 默认目标为支持原生 ES 模块、原生 ESM 动态导入和 `import.meta` 的浏览器
   - 可以通过 `build.target` 自定义目标浏览器，如 `'es2015'`
   - 使用官方插件 `@vitejs/plugin-legacy` 支持旧版浏览器

`@vitejs/plugin-legacy` 的主要功能：

- 为所有代码产生适用于旧版浏览器的版本
- 生成相应的 polyfill chunks
- 根据用户浏览器自动交付适当的版本
- 默认目标为 `'defaults'`（> 0.5%, last 2 versions, Firefox ESR, not dead）

费曼式解释：浏览器兼容性就像是为不同年龄的观众准备电影。开发环境中，Vite 假设所有观众都是年轻人（现代浏览器），可以直接播放最新的4K高清内容（esnext代码）。但在生产环境，Vite 知道可能有各种年龄的观众，所以它准备了两个版本的电影：一个是原始的4K版本（现代浏览器版本），另一个是适配老式电视的标清版（legacy版本）。当观众走进电影院（访问网站）时，检票员（浏览器特性检测）会自动引导他们去合适的放映厅，确保每个人都能看到电影，只是画质可能有所不同。这样，无论观众使用的是最新的IMAX影院还是老式的家庭电视，都能享受到电影的内容。

## Vite 如何与不同前端框架集成？

Vite 提供了与多种主流前端框架的无缝集成：

1. **Vue**：
   - 官方插件 `@vitejs/plugin-vue` 支持 Vue 3 单文件组件
   - `@vitejs/plugin-vue-jsx` 提供 Vue 3 JSX 支持
   - 对于 Vue 2，使用 `vite-plugin-vue2`

2. **React**：
   - 官方插件 `@vitejs/plugin-react` 提供 React 支持和 Fast Refresh 功能
   - 使用 Fast Refresh 技术保留组件状态
   - 支持 JSX 和 TSX 文件

3. **Svelte**：
   - 使用 `@sveltejs/vite-plugin-svelte` 插件
   - 支持热模块替换和组件预处理

4. **其他框架**：
   - Preact：使用 `@preact/preset-vite`
   - Lit：使用 `@lit/vite-plugin-lit`
   - Solid：使用 `vite-plugin-solid`
   - Elm：使用 `vite-plugin-elm`

费曼式解释：Vite 与前端框架的集成就像是一个通用适配器系统。想象你有各种不同品牌的电器（框架），每个都有自己独特的插头形状（语法和特性）。Vite 提供了一系列专用适配器（插件），让你可以轻松地将任何电器连接到同一个高效电源（Vite 的构建系统）。这样，无论你选择使用微波炉（React）、烤箱（Vue）还是电饭煲（Svelte），它们都能获得同样的高速电力供应，而你不需要理解每个电器内部的电路是如何工作的。插件会自动处理所有的转换和适配工作，让你专注于使用电器来烹饪美食（开发应用程序）。

## 如何解决 Vite 开发过程中常见的问题？

Vite 开发中可能遇到的常见问题及解决方案：

1. **热更新不工作**：
   - 检查是否使用了与 HMR 不兼容的代码拆分策略
   - 确认 `server.hmr` 配置正确，尤其是在代理后面运行时
   - 检查文件系统监听是否正常工作，可尝试设置 `server.watch`

2. **依赖预构建问题**：
   - 手动添加问题依赖到 `optimizeDeps.include`
   - 当添加新依赖后，使用 `vite --force` 重启开发服务器
   - 检查依赖是否使用了不支持的非标准语法

3. **构建错误**：
   - 检查是否使用了动态导入时的变量语法问题
   - 对于 CSS 代码分割问题，可考虑设置 `build.cssCodeSplit`
   - 对于构建超时，可增加 `build.chunkSizeWarningLimit`

4. **配置相关问题**：
   - 确保正确解析了环境变量，可使用 `console.log(import.meta.env)` 检查
   - 验证 `resolve.alias` 设置是否正确
   - 检查 Vite 插件的执行顺序，可能需要调整 `enforce` 属性

5. **浏览器兼容性问题**：
   - 确认是否正确配置了 `@vitejs/plugin-legacy`
   - 检查 `build.target` 设置是否与目标浏览器兼容

费曼式解释：解决 Vite 问题就像是修理一辆高科技自行车。当自行车（开发环境）突然不能正常工作时，你需要系统地检查各个部分。可能是链条（热更新）松了需要重新连接，可能是轮胎（依赖预构建）需要充气，也可能是变速器（配置）需要调整。重要的是了解每个部件的作用和常见故障，以及如何诊断问题。有时候，简单地重启车轮（重启服务器）就能解决问题；其他时候，你可能需要更换某个零件（调整配置或添加插件）。通过系统的排除法和了解常见故障模式，大多数问题都能够快速解决。

## Vite 的命令行界面（CLI）有哪些主要用法？

Vite 的命令行界面提供了多种命令和选项：

1. **主要命令**：
   - `vite`：启动开发服务器，别名有 `vite dev` 和 `vite serve`
   - `vite build`：构建生产版本
   - `vite preview`：本地预览生产构建
   - `vite optimize`：预构建依赖

2. **通用选项**：
   - `--config <file>`：指定配置文件路径
   - `--mode <mode>`：指定环境模式
   - `--base <path>`：指定公共基础路径
   - `--logLevel <level>`：设置日志级别（info、warn、error、silent）

3. **开发服务器特定选项**：
   - `--port <port>`：指定端口
   - `--host [host]`：指定主机（设置为 0.0.0.0 可从本地网络访问）
   - `--open [path]`：启动后自动打开浏览器
   - `--cors`：启用 CORS
   - `--strictPort`：如果端口已被使用，则退出
   - `--force`：强制重新构建依赖

4. **构建特定选项**：
   - `--outDir <dir>`：指定输出目录
   - `--emptyOutDir`：若输出目录在根目录外，强制清空
   - `--watch`：在监视模式下重建

这些命令通常在 package.json 的 scripts 字段中设置为快捷方式。

费曼式解释：Vite 的命令行界面就像是一台多功能咖啡机的控制面板。根据你按下的按钮（命令），它会制作不同类型的咖啡（开发环境、生产构建等）。主按钮（主要命令）决定了你要做什么类型的咖啡，而调节旋钮（选项）则让你可以精细控制咖啡的浓度、温度和分量（端口、主机、输出目录等）。你可以使用预设（package.json 中的脚本）快速制作常用的咖啡类型，也可以通过组合不同的按钮和旋钮设置来创建完全符合你口味的特制咖啡。整个界面设计得直观易用，即使是咖啡新手也能快速上手。
